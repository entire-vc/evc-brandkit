## Сводка проекта evc-brandkit

### Что делали

**1. Консультация по UI-фреймворкам** Провели исследование и сравнение библиотек компонентов для мультиплатформенной экосистемы Entire VC. Рекомендация: **shadcn/ui** + design tokens через Tailwind preset.

**2. Создали monorepo проект** Путь: `/Users/rogozhin/DevProjects/evc-brandkit`

**Структура:**

```
evc-brandkit/
├── packages/
│   ├── tokens/          # 🎨 Design Tokens + Tailwind preset
│   │   └── css/         # 4 готовых темы
│   └── ui/              # 🧩 18 React компонентов
└── apps/
    └── docs/            # 📚 Storybook
```

**3. Создали 4 темы:**

- `entire.css` — базовый бренд (серый `#525769`)
- `spark.css` — 🔥 оранжевый (`#FF6A3D`), dark-first
- `playground.css` — 🌊 синий (`#3D8BFF`)
- `team-relay.css` — индиго (`#6366F1`)

**4. Добавили 18 компонентов:** Button, Card, Input, Textarea, Label, Badge, Avatar, Alert, Tabs, Dialog, Select, Switch, Checkbox, RadioGroup, Progress, Skeleton, Separator, Tooltip

**5. Настроили Storybook** с stories для всех компонентов + документация по цветам и типографике

---

### На чём остановились

Ты попросил запустить проект. Я начал `pnpm install`, но процесс не завершился (либо прервался, либо ты вернулся позже).

---

### Следующие шаги

bash

```bash
cd /Users/rogozhin/DevProjects/evc-brandkit

# 1. Установить зависимости
pnpm install

# 2. Собрать пакеты
pnpm build

# 3. Запустить Storybook
pnpm dev
```

Потом планировали:

- Запушить в `github.com/entire-vc/evc-brandkit` (приватный репо)
- Сделать preset для **EVC Spark** (с нуля)
- Адаптировать **Team Relay** (Control-plane, Webpublish)

Продолжаем с установки?