# @entire-vc/* are published to the public npm registry (npmjs.org), where they
# are readable anonymously. Pinning the scope here keeps a developer's personal
# ~/.npmrc from silently redirecting it to some other registry.
@entire-vc:registry=https://registry.npmjs.org/
